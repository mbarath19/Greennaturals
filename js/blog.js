// Example JS for interactive features (if needed)
document.querySelectorAll('.btn-primary').forEach(button => {
    button.addEventListener('click', () => {
      alert("More content coming soon!");
    });
  });
  